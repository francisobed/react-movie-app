import {createTheme} from '@material-ui/core';



const theme = createTheme({
    palette: {
        primary: {
            main: "#e5989b",
        },
        secondary: {
            main: "#14213d",
        },
        common: {
            drawer: "#A22032",
        },
    },
});

export default theme;